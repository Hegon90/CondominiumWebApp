@model List<CondominiumWebApp.Models.User>
@{
    Layout = "_Login_Layout";
}

@{
    ViewData["Title"] = "User List";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card custom-background">
                <div class="card-body custom-background">
                    <h2 class="card-title text-white">Ingresar</h2>
                    <form asp-controller="User" asp-action="Details" method="post">
                        <div class="form-group">
                            <label for="email" class="control-label text-white">Usuario:</label>
                            <input type="text" name="email" class="form-control" placeholder="Nombre de usuario o correo" />
                        </div>

                        <div class="form-group">
                            <label for="password" class="text-white">Contraseña:</label>
                            <input type="password" name="password" class="form-control custom-input" />
                        </div>

                        <button type="submit" class="btn btn-success mt-3 text-white">Login</button>
                    </form>

                    @if (ViewBag.ErrorMessage != null)
                    {
                        <div class="alert alert-danger mt-2">@ViewBag.ErrorMessage</div>
                    }

                    @if (ViewBag.LoginSuccess != null)
                    {
                        <div class="alert alert-success mt-2">Login successful!</div>
                    }

                    <div class="mt-3 text-center">
                        <p class="text-white">¿Aún no tienes una cuenta?</p>
                        <a href="@Url.Action("Create", "User")" class="btn btn-primary">Registrarse</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

